{
  "name": "badminton-assistant",
  "version": "1.0.0",
  "homepage": "https://brady-static-web.github.io/BadmintonAssistant/",
  "scripts": {
    "start": "react-scripts start",
    "build": "cross-env BUILD_PATH=docs react-scripts build",
    "deploy": "git add . && git commit -m 'Deploy to docs' && git push origin main"
  },
  "dependencies": {
    "lucide-react": "^0.292.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "react-scripts": "5.0.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
